function changeColor(color, selector){
    //go and select the selector the user asked for and then set it's background color to the color the suer asked for:
    document.querySelector(selector).style.backgroundColor = color;

}